import 'package:flutter/material.dart' show Color;

/// Voxera — original portfolio brand (unique name + design)
class BrandConfig {
  BrandConfig._();

  static const String appName = 'Voxera';
  static const String tagline = 'Speak. Chat. Create.';
  static const String chatHint = 'Ask Voxera anything...';
  static const String logoAsset = 'assets/logo/logo.png';

  static const Color primary = Color(0xFF6366F1);
  static const Color primaryDark = Color(0xFF4F46E5);
}
